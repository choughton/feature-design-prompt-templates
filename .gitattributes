# Normalize line endings to LF on commit, checkout as OS-native
* text=auto

# Force LF for markdown and config files (consistency across OS)
*.md text eol=lf
*.txt text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
